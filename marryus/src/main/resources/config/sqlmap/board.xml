<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.marryus.repository.mapper.BoardMapper">
	<resultMap id="boardMap" type="board">
		<result column="board_no" property="boardNo" />
		<result column="writer" property="writer" />
		<result column="title" property="title" />
		<result column="content" property="content" />
		<result column="reg_date" property="regDate" />
		<result column="view_cnt" property="viewCnt"/>
		<result column="category" property="category" />
		<result column="answer" property="answer" />
	</resultMap>
	
	<resultMap id="CommentMap" type="Comment">
		<result column="comm_no" property="commNo" />
		<result column="board_no" property="boardNo" />
		<result column="comm_writetr" property="commWriter" />
		<result column="comm_content" property="commContent" />
		<result column="comm_date" property="commDate" />
	</resultMap>
	
	
	
	<!-- 공지게시판 리스트(페이징) -->
	<select id="boardList" parameterType="Page" resultMap="boardMap">
		select @rownum:=@rownum+1 as no, b.*, (select count(*) from mu_board where category = 'nt') as cnt
		  from mu_board b, (select @rownum:=0) TMP
		 where category = 'nt'
		 order by no desc
		 limit #{begin}, 10
	</select>
	<!-- 공지게시판 디테일 -->
	<select id="noticeDetailBoard" parameterType="int" resultMap="boardMap">
		select *
		from mu_board
		where board_no = #{boardNo}
	</select>
	<!-- 공지게시판 조회수 -->
	<update id="listNoticeUpdateView">
		update mu_board
		set view_cnt = view_cnt +1
		where board_no = #{boardNo}
	</update>
	
	<select id="selectBoardCount" resultType="int">
		select count(*)
		from mu_board
		where category = 'nt'
	</select>
	
	
	
	<!-- 1대1 게시판 -->
	<select id="mtomList" parameterType="Page" resultMap="boardMap">
		select @rownum:=@rownum+1 as no, b.*, (select count(*) from mu_board where category = 'mm') as cnt
		  from mu_board b, (select @rownum:=0) TMP
		 where category = 'mm'
		 order by no desc
		 limit #{begin}, 10
	</select>
	<!-- 1대1 게시판 상세 -->
	<select id="mtomDetailBoard" parameterType="int" resultMap="boardMap">
		select *
		from mu_board
		where board_no = #{boardNo}
	</select>
	<!-- 조회수 -->
	<update id="listMtoMUpdateView">
		update mu_board
		set view_cnt = view_cnt +1
		where board_no = #{boardNo}
	</update>
	
	<select id="selectMtoMBoardCount" resultType="int">
		select count(*)
		from mu_board
		where category = 'mm'
	</select>
	<!-- 1대1 질문 글쓰기 -->
	<insert id="insertMtom" parameterType="board">
	insert into mu_board(title, writer, content, category)
		values(#{title}, #{writer}, #{content}, 'mm')
	</insert>
	
	<!-- 1대1 게시판 삭제 -->
	<delete id="mtomdelete" parameterType="int">
		delete from mu_board
		where board_no = #{boardNo}
	</delete>
	
	<!-- 1대1 질문 수정 -->
	<update id="mtomUpdate" parameterType="board">
		update mu_board 
	    	set title = #{title},
			content = #{content}
	        where board_no = #{boardNo}
	</update>
	
	
	
	<!-- 댓글 -->
	<select id="listComment" parameterType="int" resultMap="CommentMap">
		select *
		  from mu_comment
		 where board_no= #{boardNo}
		order by comm_no desc
	</select>
	
	
</mapper>
























