<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.marryus.repository.mapper.BoardMapper">
	<resultMap id="boardMap" type="board">
		<result column="board_no" property="boardNo" />
		<result column="writer" property="writer" />
		<result column="title" property="title" />
		<result column="content" property="content" />
		<result column="reg_date" property="regDate" />
		<result column="view_cnt" property="viewCnt"/>
		<result column="category" property="category" />
		<result column="answer" property="answer" />
	</resultMap>
	
	<select id="boardList" parameterType="Page" resultMap="boardMap">
		select a.*,(select count(*) from mu_board where category = 'nt') as cnt
      from mu_board a
      where category = 'nt'
      order by board_no desc
	limit #{begin}, 10
	</select>
	
	<select id="noticeDetailBoard" parameterType="int" resultMap="boardMap">
		select *
		from mu_board
		where board_no = #{boardNo}
	</select>
	
	<update id="listNoticeUpdateView">
		update mu_board
		set view_cnt = view_cnt +1
		where board_no = #{boardNo}
	</update>
	
	
	
	<select id="selectBoardCount" resultType="int">
		select count(*)
		from mu_board
		where category = 'nt'
	</select>
	
	<!-- 검색 -->
	<select id="categoryList" parameterType="Page2" resultMap="boardMap" useCache="false" flushCache="true">
		select a.*,(select count(*) from mu_board where category = 'nt') as cnt
		 from mu_board a
		 <choose>
			<when test="select=='title'" >
			where title like concat('%',#{text},'%')
			</when>
			<otherwise>
			where concat(a.title,a.content) like concat('%',#{text},'%')
			</otherwise>
		</choose>
			limit #{begin} , 10
	</select>
	
	<select id="categoryCount" parameterType="Page2" resultType="int">
		select count(*)
		from mu_board 
		<choose>
			<when test="select=='title' " >
			where title like concat('%',#{text},'%')
			</when>
			<otherwise>
			where concat(title,content) like concat('%',#{text},'%')
			</otherwise>
		</choose>
</select>
	
</mapper>
























